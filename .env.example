# Raspberry Pi Connection
PI_HOST=btbg.local
PI_USER=yze
BTBG_PORT=9090

# Vite frontend (must be prefixed with VITE_ to be exposed to the app)
# Change VITE_PI_HOST to your Pi's IP if btbg.local doesn't work
VITE_PI_HOST=btbg.local
VITE_BTBG_PORT=9090
